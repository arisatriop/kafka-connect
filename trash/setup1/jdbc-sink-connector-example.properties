name=jdbc-sink-postgres-src_db-products
connector.class=io.confluent.connect.jdbc.JdbcSinkConnector
tasks.max=1

connection.url=jdbc:postgresql://host.docker.internal:5432/dst_db
connection.user=postgres
connection.password=postgres

insert.mode=upsert
pk.mode=record_value
pk.fields=id
delete.enabled=true

auto.create=false
auto.evolve=false

table.name.format=products
topics=dbz-source-postgres-src_db-products.public.products